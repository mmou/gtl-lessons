var express = require('express');
var router = express.Router();
var utils = require('../utils/utils')

router.get('/', function(req, res) {
  // TODO server side template rendering
});

module.exports = router;